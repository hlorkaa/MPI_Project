<!doctype html>
<html>
<head>
    <style>
        .styled-table {
            border-collapse: collapse;
            margin: 25px 0;
            font-size: 0.9em;
            font-family: sans-serif;
            min-width: 400px;
            box-shadow: 0 0 20px rgba(0, 0, 0, 0.15);
        }
        .styled-table thead tr {
            background-color: #009879;
            color: #000000;
            text-align: left;
            position: sticky;
            top: 0;
            font-size: 0.9em;
        }
        .styled-table th,
        .styled-table td {
            padding: 12px 15px;
            height: 20px;
        }
        .styled-table tbody tr {
            border-bottom: 1px solid #dddddd;
            vertical-align: top;
            background-color: #f3f3f3;
        }
        .styled-table tbody tr:nth-of-type(even) {
            background-color: #f3f3f3;
        }
        .styled-table tbody tr:last-of-type {
            border-bottom: 2px solid #009879;
        }
        .styled-table tbody tr.active-row {
            font-weight: bold;
            color: #009879;
        }
        .styled-table td.rightСol {
            text-align: right;
        }
        .styled-table button.colBut{
            height: 20px;
            width: 70px;
            font-size: 15px;
        }
        .styled-table td.colUser {
            padding: 2px 15px;
            height: 5px;
        }
        .styled-table div.colUser {
            height:500px;
            max-width: 600px;
            overflow:auto;
        }
        .styled-table td.colReport {
            padding: 2px 15px;
            height: 5px;
        }
        .styled-table div.colReport {
            height:200px;
            max-width: 1200px;
            overflow:auto;
        }
        .styled-table button.colReport {
            white-space: nowrap;
        }
        .styled-table td.colTask {
            padding: 2px 15px;
            height: 5px;
        }
        .styled-table div.colTask {
            height:200px;
            max-width: 1200px;
            overflow:auto;
        }
        .styled-table button.colTask {
            white-space: nowrap;
        }
        .styled-table td.colOrder {
            padding: 2px 15px;
            height: 5px;
        }
        .styled-table div.colOrder {
            height:200px;
            max-width: 800px;
            overflow:auto;
        }
        .styled-table button.colOrder {
            white-space: nowrap;
        }
        .styled-table input[type=text],
        .styled-table input[type=password] {
            width: 200px;
        }
    </style>
</head>
<body>